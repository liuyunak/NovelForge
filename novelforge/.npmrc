# pnpm configuration
# Approve build scripts for trusted packages (pnpm >= 10 requires explicit
# approval for packages with install-time build scripts).
# @biomejs/biome and esbuild are well-known, widely-used dev tools.
onlyBuiltDependencies[]= @biomejs/biome
onlyBuiltDependencies[]= esbuild
